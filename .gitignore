# deps / build
node_modules/
dist/

# large drop-in binaries — kept out of the source branch for size; bundled at
# deploy time. Models are free-to-use (see CREDITS.md for attribution).
public/car.glb
public/wheel.glb
public/engine.wav
public/textures/

# OSM/LiDAR build intermediates (processed result ships in src/track.json)
spa_raw.json
spa_rel.json
spa_ways.json
spa_stands.json
spa_centerline.json

.DS_Store
